if __name__ == '__main__':
    # Python f-strings are not supported in Python 3.5, 
    # so this will fail for Python 3.5.
    print(f"Hello world! This is printed from a Python script")
    print(f"Asserting True.")
    assert True
